# 黄金分析现状与宏观因素改进建议

## 📋 文档说明

本文档分析当前系统中黄金分析功能的实现现状，特别是关于"全球宏观因素"的处理情况，并提供详细的改进建议。

**创建日期**: 2026-02-10  
**相关文件**: 
- [src/gold_analyzer.py](../src/gold_analyzer.py) - 黄金趋势分析器
- [src/stock_analyzer.py](../src/stock_analyzer.py) - 股票趋势分析器（基类）
- [src/search_service.py](../src/search_service.py) - 新闻搜索服务
- [src/core/pipeline.py](../src/core/pipeline.py) - 分析流水线

---

## 1. 当前黄金分析实现现状

### 1.1 架构设计

当前系统采用**面向对象继承**的方式实现黄金分析：

```
┌─────────────────────────────────────────────────────────────┐
│                    StockTrendAnalyzer                       │
│                    (股票趋势分析器 - 基类)                   │
├─────────────────────────────────────────────────────────────┤
│  功能: 均线分析、MACD、RSI、量能分析、乖离率计算              │
│  参数: BIAS_THRESHOLD=5.0%, VOLUME_HEAVY_RATIO=1.5          │
└─────────────────────────────────────────────────────────────┘
                              ▲
                              │ 继承
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    GoldTrendAnalyzer                        │
│                    (黄金趋势分析器 - 子类)                   │
├─────────────────────────────────────────────────────────────┤
│  重写方法: _analyze_volume(), _analyze_trend()              │
│           _generate_signal(), format_analysis()             │
│  调整参数: BIAS_THRESHOLD=3.0%, VOLUME_HEAVY_RATIO=1.8      │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 已实现功能

| 功能模块 | 实现状态 | 说明 |
|---------|---------|------|
| **技术分析** | ✅ 完整实现 | 均线、MACD、RSI、乖离率、量能 |
| **趋势判断** | ✅ 完整实现 | 多头/空头排列识别 |
| **买入信号** | ✅ 完整实现 | 基于技术面的买卖建议 |
| **参数优化** | ✅ 已实现 | 针对黄金调整阈值 |
| **全球宏观因素** | ❌ **未实现** | 仅输出静态提示文本 |

### 1.3 代码差异对比

#### 参数差异

```python
# src/stock_analyzer.py - 股票分析器参数
class StockTrendAnalyzer:
    BIAS_THRESHOLD = 5.0        # 乖离率阈值 5%
    VOLUME_HEAVY_RATIO = 1.5    # 放量判断阈值 1.5
    VOLUME_SHRINK_RATIO = 0.7   # 缩量判断阈值 0.7

# src/gold_analyzer.py - 黄金分析器参数
class GoldTrendAnalyzer(StockTrendAnalyzer):
    BIAS_THRESHOLD = 3.0        # 乖离率阈值 3%（黄金波动较小）
    VOLUME_HEAVY_RATIO = 1.8    # 放量判断阈值 1.8（黄金交易特性）
    VOLUME_SHRINK_RATIO = 0.7   # 缩量判断阈值 0.7（保持不变）
```

#### 方法重写（4个方法）

| 方法名 | 重写内容 | 目的 |
|--------|---------|------|
| `_analyze_volume()` | 调整放量阈值，添加"(黄金)"标识 | 适配黄金量能特性 |
| `_analyze_trend()` | 添加黄金趋势特性说明 | 提示黄金趋势持久性 |
| `_generate_signal()` | 添加避险资产特性分析 | 增强信号可靠性说明 |
| `format_analysis()` | 添加黄金市场提示 | 输出格式优化 |

#### 继承的方法（未修改）

以下方法完全继承自股票分析器：
- `_calculate_mas()` - 计算均线
- `_calculate_macd()` - 计算 MACD
- `_calculate_rsi()` - 计算 RSI
- `_calculate_bias()` - 计算乖离率
- `_analyze_support_resistance()` - 支撑压力分析
- `_analyze_macd()` - MACD 分析
- `_analyze_rsi()` - RSI 分析

---

## 2. 全球宏观因素处理现状

### 2.1 当前实现：仅静态文本提示

在 `gold_analyzer.py` 第 191-193 行，系统仅输出静态提示：

```python
# 添加黄金特有的分析提示
lines.append(f"")
lines.append(f"💡 黄金市场提示:")
lines.append(f"   - 黄金作为避险资产，在市场不确定性增加时往往表现强势")
lines.append(f"   - 黄金价格受全球宏观经济、地缘政治等因素影响较大")
lines.append(f"   - 黄金趋势一旦形成，往往持续时间较长")
```

**问题**: 这些提示只是**固定的文本描述**，没有实际数据支撑，无法根据当前宏观环境动态调整分析结果。

### 2.2 缺失的宏观因素

黄金价格受以下全球宏观因素影响，但当前系统**均未采集和分析**：

```
┌────────────────────────────────────────────────────────────────┐
│                    黄金价格关键宏观驱动因素                     │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  1. 美联储货币政策                                              │
│     ├── 联邦基金利率决策                                        │
│     ├── 加息/降息预期（点阵图）                                 │
│     └── 美联储官员讲话（鲍威尔等）                              │
│                                                                │
│  2. 美元指数 (DXY)                                              │
│     ├── 美元强弱与黄金负相关                                    │
│     └── 影响黄金的国际购买力                                    │
│                                                                │
│  3. 实际利率                                                    │
│     ├── 名义利率 - 通胀预期                                     │
│     └── 实际利率上升 → 黄金下跌                                 │
│                                                                │
│  4. 通胀数据                                                    │
│     ├── 美国 CPI（消费者价格指数）                              │
│     ├── 美国 PCE（个人消费支出）                                │
│     └── 通胀预期调查                                            │
│                                                                │
│  5. 地缘政治风险                                                │
│     ├── 国际冲突（战争、制裁）                                  │
│     ├── 政治不确定性（选举、政变）                              │
│     └── 贸易摩擦                                                │
│                                                                │
│  6. 央行购金数据                                                │
│     ├── 各国央行黄金储备变化                                    │
│     ├── 世界黄金协会季度报告                                    │
│     └── 央行购金趋势                                            │
│                                                                │
│  7. 全球经济形势                                                │
│     ├── 经济增长预期（GDP）                                     │
│     ├── 经济衰退风险                                            │
│     └── 金融市场波动率（VIX）                                   │
│                                                                │
│  8. 其他贵金属市场                                              │
│     ├── 白银价格走势                                            │
│     ├── 铂金、钯金价格                                          │
│     └── 金银比                                                  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### 2.3 新闻搜索现状

当前 `search_service.py` 的搜索查询针对**股票通用新闻**：

```python
# src/market_analyzer.py - 当前搜索查询
search_queries = [
    "A股 大盘 复盘",
    "股市 行情 分析",
    "A股 市场 热点 板块",
]
```

**缺失**: 没有针对黄金的宏观因素专用搜索。

---

## 3. 改进方案建议

### 方案一：轻度改进 - 扩展新闻搜索（推荐短期）

**目标**: 在现有搜索服务基础上，增加黄金宏观因素搜索维度

**实施步骤**:

1. **在 `search_service.py` 中添加黄金专用搜索查询**

```python
# 黄金专用宏观因素搜索查询
GOLD_MACRO_SEARCH_QUERIES = {
    "美联储政策": [
        "美联储利率决议 黄金影响",
        "美联储加息降息预期",
        "鲍威尔讲话 黄金价格",
        "美联储点阵图 黄金",
    ],
    "美元指数": [
        "美元指数 DXY 走势 黄金",
        "美元强弱 黄金价格关系",
    ],
    "通胀数据": [
        "美国CPI数据 黄金反应",
        "美国PCE通胀 黄金价格",
        "通胀预期 黄金走势",
    ],
    "地缘政治": [
        "地缘政治风险 避险资产",
        "国际冲突 黄金避险需求",
        "全球经济不确定性 黄金",
    ],
    "央行购金": [
        "央行购金 黄金储备",
        "世界黄金协会 央行黄金需求",
    ],
}
```

2. **在 `GoldTrendAnalyzer` 中集成搜索结果**

```python
class GoldTrendAnalyzer(StockTrendAnalyzer):
    def analyze_with_macro(self, df, code, macro_news=None):
        """带宏观因素的黄金分析"""
        # 1. 执行基础技术分析
        result = self.analyze(df, code)
        
        # 2. 如果有宏观新闻，添加分析
        if macro_news:
            macro_score = self._analyze_macro_factors(macro_news)
            result.signal_score = self._adjust_score_by_macro(
                result.signal_score, 
                macro_score
            )
            result.macro_factors = macro_news
            
        return result
    
    def _analyze_macro_factors(self, news):
        """分析宏观因素对黄金的影响"""
        # 基于新闻内容评分
        bullish_keywords = ['加息', '通胀上升', '地缘政治', '避险']
        bearish_keywords = ['降息', '经济强劲', '美元走强']
        
        # 计算宏观得分
        score = 50  # 中性
        for news_item in news:
            content = news_item.get('content', '')
            if any(kw in content for kw in bullish_keywords):
                score += 10
            if any(kw in content for kw in bearish_keywords):
                score -= 10
                
        return max(0, min(100, score))
```

**优点**:
- 无需新增数据源
- 利用现有搜索服务
- 实现成本低

**缺点**:
- 依赖新闻质量
- 无法获取结构化数据（如具体利率数值）

---

### 方案二：中度改进 - 接入宏观数据源（推荐中期）

**目标**: 接入专门的宏观数据 API，获取结构化数据

**推荐数据源**:

| 数据源 | 可获取数据 | 接入难度 | 成本 |
|--------|-----------|---------|------|
| **FRED API** (美联储) | 利率、通胀、就业数据 | 中等 | 免费 |
| **Yahoo Finance** | DXY美元指数、国债收益率 | 简单 | 免费 |
| **World Gold Council** | 央行购金数据、黄金需求 | 较难 | 需申请 |
| **Investing.com API** | 财经日历、市场数据 | 中等 | 部分免费 |

**实施步骤**:

1. **创建宏观数据获取模块** `src/macro_data_provider.py`

```python
# -*- coding: utf-8 -*-
"""
===================================
宏观数据获取模块 - 黄金价格影响因素
===================================
"""

import logging
import requests
import pandas as pd
from typing import Optional, Dict, Any
from datetime import datetime, timedelta

logger = logging.getLogger(__name__)


class MacroDataProvider:
    """宏观数据提供者"""
    
    def __init__(self):
        self.fred_api_key = None  # 从配置读取
        self.cache = {}
    
    def get_dxy_index(self, days: int = 30) -> Optional[pd.DataFrame]:
        """
        获取美元指数 (DXY) 数据
        
        数据来源: Yahoo Finance
        代码: DX-Y.NYB
        """
        try:
            import yfinance as yf
            ticker = yf.Ticker("DX-Y.NYB")
            df = ticker.history(period=f"{days}d")
            return df
        except Exception as e:
            logger.error(f"获取DXY数据失败: {e}")
            return None
    
    def get_us_treasury_yield(self, maturity: str = "10Y") -> Optional[float]:
        """
        获取美国国债收益率
        
        Args:
            maturity: 期限 ("2Y", "5Y", "10Y", "30Y")
        """
        try:
            # 使用Yahoo Finance获取国债ETF
            treasury_map = {
                "2Y": "SHY",   # 1-3年期国债ETF
                "5Y": "IEI",   # 3-7年期国债ETF
                "10Y": "IEF",  # 7-10年期国债ETF
                "30Y": "TLT",  # 20+年期国债ETF
            }
            import yfinance as yf
            ticker = yf.Ticker(treasury_map.get(maturity, "IEF"))
            info = ticker.info
            # 计算收益率近似值
            return info.get('yield', None)
        except Exception as e:
            logger.error(f"获取国债收益率失败: {e}")
            return None
    
    def get_fed_rate_decision(self) -> Dict[str, Any]:
        """
        获取美联储最新利率决议信息
        
        数据来源: FRED API 或 网络爬虫
        """
        # 实现FRED API调用
        # 或爬取美联储官网
        pass
    
    def get_central_bank_gold_purchases(self) -> pd.DataFrame:
        """
        获取各国央行购金数据
        
        数据来源: 世界黄金协会
        """
        # 实现世界黄金协会数据获取
        pass
    
    def get_geopolitical_risk_index(self) -> float:
        """
        获取地缘政治风险指数
        
        数据来源: 第三方服务或基于新闻计算
        """
        # 实现地缘政治风险评分
        pass


class GoldMacroAnalyzer:
    """黄金宏观因素分析器"""
    
    def __init__(self):
        self.data_provider = MacroDataProvider()
    
    def get_macro_score(self) -> Dict[str, Any]:
        """
        获取综合宏观因素评分
        
        Returns:
            {
                "total_score": 65,  # 0-100，越高越利好黄金
                "factors": {
                    "dxy": {"value": 103.5, "impact": "bearish", "score": 40},
                    "fed_rate": {"value": 5.25, "impact": "neutral", "score": 50},
                    "inflation": {"value": 3.2, "impact": "bullish", "score": 70},
                    # ...
                },
                "summary": "美元强势压制黄金，但通胀支撑价格"
            }
        """
        factors = {}
        
        # 1. 美元指数影响
        dxy_data = self.data_provider.get_dxy_index(days=5)
        if dxy_data is not None and len(dxy_data) >= 2:
            dxy_current = dxy_data['Close'].iloc[-1]
            dxy_change = (dxy_current - dxy_data['Close'].iloc[-2]) / dxy_data['Close'].iloc[-2]
            
            # 美元上涨 → 利空黄金
            if dxy_change > 0.01:
                dxy_score = 30  # 利空
                dxy_impact = "bearish"
            elif dxy_change < -0.01:
                dxy_score = 70  # 利好
                dxy_impact = "bullish"
            else:
                dxy_score = 50  # 中性
                dxy_impact = "neutral"
                
            factors["dxy"] = {
                "value": round(dxy_current, 2),
                "change": round(dxy_change * 100, 2),
                "impact": dxy_impact,
                "score": dxy_score
            }
        
        # 2. 实际利率影响
        treasury_yield = self.data_provider.get_us_treasury_yield("10Y")
        if treasury_yield:
            # 实际利率 = 名义利率 - 通胀预期（简化计算）
            # 实际利率上升 → 利空黄金
            real_rate = treasury_yield - 2.5  # 假设通胀预期2.5%
            
            if real_rate > 2.0:
                rate_score = 20
                rate_impact = "strongly_bearish"
            elif real_rate > 1.0:
                rate_score = 35
                rate_impact = "bearish"
            elif real_rate > 0:
                rate_score = 50
                rate_impact = "neutral"
            else:
                rate_score = 75
                rate_impact = "bullish"
                
            factors["real_rate"] = {
                "value": round(real_rate, 2),
                "impact": rate_impact,
                "score": rate_score
            }
        
        # 3. 计算综合得分
        if factors:
            total_score = sum(f["score"] for f in factors.values()) / len(factors)
        else:
            total_score = 50
            
        # 4. 生成总结
        summary = self._generate_summary(factors)
        
        return {
            "total_score": round(total_score),
            "factors": factors,
            "summary": summary,
            "timestamp": datetime.now().isoformat()
        }
    
    def _generate_summary(self, factors: Dict) -> str:
        """生成宏观因素总结"""
        bullish_count = sum(1 for f in factors.values() if f["score"] > 55)
        bearish_count = sum(1 for f in factors.values() if f["score"] < 45)
        
        if bullish_count > bearish_count:
            return f"宏观环境整体利好黄金（{bullish_count}项利好 vs {bearish_count}项利空）"
        elif bearish_count > bullish_count:
            return f"宏观环境整体利空黄金（{bullish_count}项利好 vs {bearish_count}项利空）"
        else:
            return "宏观环境中性，关注技术面信号"
```

2. **在 `GoldTrendAnalyzer` 中集成宏观分析**

```python
class GoldTrendAnalyzer(StockTrendAnalyzer):
    def __init__(self):
        super().__init__()
        self.macro_analyzer = GoldMacroAnalyzer()
    
    def analyze(self, df, code):
        # 1. 执行基础技术分析
        result = super().analyze(df, code)
        
        # 2. 获取宏观因素评分
        try:
            macro_data = self.macro_analyzer.get_macro_score()
            result.macro_score = macro_data["total_score"]
            result.macro_factors = macro_data["factors"]
            result.macro_summary = macro_data["summary"]
            
            # 3. 调整综合评分
            # 技术评分权重 60%，宏观评分权重 40%
            result.signal_score = int(result.signal_score * 0.6 + macro_data["total_score"] * 0.4)
            
        except Exception as e:
            logger.warning(f"获取宏观数据失败: {e}")
            result.macro_score = None
            
        return result
    
    def format_analysis(self, result):
        # 基础格式化
        lines = super().format_analysis(result)
        
        # 添加宏观因素分析
        if hasattr(result, 'macro_score') and result.macro_score:
            lines.append(f"")
            lines.append(f"🌍 宏观因素分析:")
            lines.append(f"   综合评分: {result.macro_score}/100")
            lines.append(f"   分析总结: {result.macro_summary}")
            
            if result.macro_factors:
                lines.append(f"   关键因素:")
                for factor_name, factor_data in result.macro_factors.items():
                    emoji = "📈" if factor_data["score"] > 55 else "📉" if factor_data["score"] < 45 else "➡️"
                    lines.append(f"   {emoji} {factor_name}: {factor_data['value']} ({factor_data['impact']})")
        
        return "\n".join(lines)
```

**优点**:
- 获取结构化宏观数据
- 可量化宏观因素影响
- 分析结果更专业

**缺点**:
- 需要接入新数据源
- 实现复杂度较高
- 需要维护数据接口

---

### 方案三：深度改进 - AI 驱动的宏观分析（推荐长期）

**目标**: 利用大语言模型分析宏观因素对黄金的影响

**实施步骤**:

1. **创建 AI 宏观分析模块**

```python
class AIGoldMacroAnalyzer:
    """AI驱动的黄金宏观分析器"""
    
    def __init__(self, analyzer):
        self.analyzer = analyzer  # GeminiAnalyzer实例
    
    def analyze_macro_impact(self, news_context: str, macro_data: Dict) -> str:
        """
        使用AI分析宏观因素对黄金的影响
        
        Args:
            news_context: 新闻搜索结果
            macro_data: 结构化宏观数据
            
        Returns:
            AI生成的宏观分析报告
        """
        prompt = f"""
        作为黄金投资专家，请基于以下宏观数据和市场新闻，分析当前宏观环境对黄金价格的影响：
        
        【宏观数据】
        - 美元指数: {macro_data.get('dxy', 'N/A')}
        - 10年期美债收益率: {macro_data.get('treasury_10y', 'N/A')}
        - 实际利率估算: {macro_data.get('real_rate', 'N/A')}
        
        【市场新闻】
        {news_context}
        
        请提供以下分析：
        1. 当前宏观环境对黄金的整体影响（利好/利空/中性）
        2. 关键驱动因素分析
        3. 短期（1-2周）价格走势预判
        4. 投资建议
        
        请用中文回答，保持简洁专业。
        """
        
        return self.analyzer.generate_response(prompt)
```

2. **整合到分析流水线**

```python
# 在 pipeline.py 中
class StockAnalysisPipeline:
    def analyze_gold(self, code: str):
        # 1. 获取黄金历史数据
        df = self.fetcher_manager.get_daily_data(code)
        
        # 2. 技术分析
        trend_result = self.gold_trend_analyzer.analyze(df, code)
        
        # 3. 获取宏观数据
        macro_data = self.macro_analyzer.get_macro_score()
        
        # 4. 搜索宏观新闻
        macro_news = self.search_service.search_gold_macro_news()
        
        # 5. AI综合分析
        ai_analysis = self.ai_macro_analyzer.analyze_macro_impact(
            macro_news, 
            macro_data
        )
        
        # 6. 整合结果
        return {
            "technical": trend_result,
            "macro": macro_data,
            "ai_analysis": ai_analysis
        }
```

**优点**:
- 分析结果智能化
- 可处理非结构化信息
- 分析维度更全面

**缺点**:
- 依赖AI模型能力
- 响应时间可能较长
- API成本较高

---

## 4. 实施路线图

```
┌─────────────────────────────────────────────────────────────────┐
│                      黄金宏观分析实施路线图                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  第一阶段（1-2周）: 轻度改进                                      │
│  ├─ 扩展搜索查询，增加黄金宏观关键词                              │
│  ├─ 在GoldTrendAnalyzer中集成新闻搜索结果                         │
│  └─ 输出格式优化，显示宏观因素提示                                │
│                                                                 │
│  第二阶段（3-4周）: 中度改进                                      │
│  ├─ 接入Yahoo Finance获取DXY美元指数                              │
│  ├─ 接入FRED API获取利率数据                                      │
│  ├─ 创建GoldMacroAnalyzer模块                                     │
│  └─ 实现宏观因素量化评分                                          │
│                                                                 │
│  第三阶段（5-8周）: 深度改进                                      │
│  ├─ 接入世界黄金协会央行购金数据                                  │
│  ├─ 开发AI宏观分析模块                                            │
│  ├─ 整合技术+宏观+AI三重分析                                      │
│  └─ 优化评分权重和算法                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. 预期效果对比

### 当前输出示例

```
=== GC=F 黄金趋势分析 ===

📊 趋势判断: 弱势空头
   均线排列: 弱势空头，MA5<MA10 但 MA10≥MA20
   趋势强度: 40/100

📈 均线数据:
   现价: 4878.70
   MA5:  4887.44 (乖离 -0.18%)
   ...

💡 黄金市场提示:
   - 黄金作为避险资产，在市场不确定性增加时往往表现强势
   - 黄金价格受全球宏观经济、地缘政治等因素影响较大  ← 静态文本，无实际数据
   - 黄金趋势一旦形成，往往持续时间较长
```

### 改进后预期输出

```
=== GC=F 黄金趋势分析 ===

📊 趋势判断: 弱势空头
   均线排列: 弱势空头，MA5<MA10 但 MA10≥MA20
   趋势强度: 40/100

📈 均线数据:
   现价: 4878.70
   MA5:  4887.44 (乖离 -0.18%)
   ...

🌍 宏观因素分析:                    ← 新增
   综合评分: 62/100 (利好)
   分析总结: 美元强势压制黄金，但通胀预期支撑价格
   
   关键因素:
   📈 美元指数: 103.5 (下跌-0.3%，利好黄金)
   📉 实际利率: 1.8% (偏高，利空黄金)
   📈 通胀预期: 3.2% (上升，利好黄金)
   📈 央行购金: Q4增加228吨 (利好黄金)

📰 宏观新闻摘要:                   ← 新增
   • 美联储暗示2024年可能降息3次
   • 地缘政治紧张局势支撑避险需求
   • 中国央行连续第18个月增持黄金

🎯 操作建议: 持有
   技术评分: 45/100
   宏观评分: 62/100
   综合评分: 52/100 (技术偏弱但宏观支撑)
```

---

## 6. 风险评估与应对

| 风险点 | 影响 | 应对措施 |
|--------|------|---------|
| 宏观数据源不稳定 | 分析中断 | 多数据源备份，降级到新闻分析 |
| API调用成本增加 | 运营成本上升 | 缓存机制，控制调用频率 |
| 宏观因素权重不当 | 分析偏差 | A/B测试，持续优化权重 |
| 数据延迟 | 实时性不足 | 明确数据时效，设置有效期 |

---

## 7. 结论与建议

### 当前现状总结

1. **技术分析已完善**: 黄金分析器已针对黄金特性优化参数
2. **宏观因素缺失**: 仅输出静态提示，无实际数据支撑
3. **改进空间巨大**: 接入宏观数据可显著提升分析质量

### 推荐实施策略

- **短期（1-2周）**: 实施方案一，扩展新闻搜索
- **中期（1个月）**: 实施方案二，接入DXY和利率数据
- **长期（2个月）**: 考虑方案三，AI驱动深度分析

### 预期收益

- 分析结果从**纯技术**升级到**技术+宏观**双维度
- 为用户提供**更专业、更全面**的黄金投资建议
- 提升系统在金融分析领域的**竞争力**

---

## 8. 参考资源

### 数据源
- [FRED API](https://fred.stlouisfed.org/docs/api/fred/) - 美联储经济数据
- [Yahoo Finance](https://finance.yahoo.com/) - 美元指数、国债收益率
- [World Gold Council](https://www.gold.org/) - 央行购金数据

### 技术文档
- [黄金价格影响因素研究](https://www.gold.org/goldhub/data/gold-demand-by-country) - 世界黄金协会
- [美联储货币政策](https://www.federalreserve.gov/monetarypolicy.htm) - 美联储官网

---

**文档维护**: 建议每季度更新一次，跟进宏观数据源变化和分析效果优化。
